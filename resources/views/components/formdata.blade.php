<div class="row">
    <i class="fas fa-book"></i>
    <select name="{{$name}}" id="{{$id}}" class="form-control input-sm" required>
        {{-- @foreach ($getcategory as $id => $name)
            <option value="{{ $id }}">{{ $name }}</option>
        @endforeach --}}
    </select>
   
</div> 